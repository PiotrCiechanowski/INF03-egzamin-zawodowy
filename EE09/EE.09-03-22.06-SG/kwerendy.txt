SELECT imie, nazwisko, punkty FROM `klienci` ORDER BY punkty DESC LIMIT 3;
SELECT typy.nazwa, COUNT(klienci.Typy_id) FROM typy JOIN klienci ON typy.id = klienci.Typy_id GROUP BY klienci.Typy_id;
SELECT klienci.zdjecie, klienci.imie, opinie.opinia FROM `klienci` JOIN opinie ON klienci.id = opinie.Klienci_id 
WHERE klienci.Typy_id = 2 OR klienci.Typy_id = 3;
ALTER TABLE klienci ADD COLUMN adres VARCHAR(50);